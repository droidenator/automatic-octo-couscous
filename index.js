function initialize(floors, elevators) {
  // Create and init elevators
  // Create floors
}

function callElevator(currentFloor, destinationFloor) {
  // Current or destination floor must be between 1 and max # of floors
  // Emit request for elevator to all elevators
  // Pick best elevator from responses
}

function Elevator(startingFloor = 1) {
  // Store number of trips and floors
  // Emit message when moving to a new floor
  // Emit message when opening doors
}
